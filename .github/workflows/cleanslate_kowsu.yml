name: Cleanslate KowSU
permissions:
  contents: write
  actions: write

on:
  workflow_call:
    inputs:
      codename:
        required: true
        type: string
      repo:
        required: true
        type: string
      android_version:
        required: true
        type: string
      kernel_version:
        required: true
        type: string
      release_tag:
        required: true
        type: string

jobs:
  build-kernel-cleanslate-kowsu-susfs:
    runs-on: ubuntu-latest
    env:
      ARCH: arm64
      CROSS_COMPILE: aarch64-linux-gnu-
      CONFIG: google_common_aosp_simple
      OUT_DIR: out          # <-- unified name

    steps:
      - name: Installing dependencies
        run: |
          sudo apt update
          sudo apt install -y jq gh curl zip unzip git libelf-dev gcc-aarch64-linux-gnu

      - name: Set CONFIG vars
        run: |
          echo "CONFIG=$CONFIG" >> "$GITHUB_ENV"
          echo "OUT_DIR=$OUT_DIR" >> "$GITHUB_ENV"

      # ------------------------------------------------------------------
      #  Clone repos (unchanged)
      # ------------------------------------------------------------------
      - name: Clone repos
        run: |
          git clone https://github.com/TheWildJames/AnyKernel3.git -b gki-2.0
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android14-6.1
          git clone https://github.com/tbalden/google_common_aosp_simple.git -b aosp_cleanslate_a16_shusky
          git clone https://github.com/tbalden/google_common_kernel_drivers_simple.git -b cleanslate_shusky_a16 tbalden_all
          git clone https://github.com/yapixel/kernel_patches.git
          git clone https://github.com/KOWX712/files.git kowsu_patches

          cd google_common_aosp_simple
          mkdir -p devices
          cp -r ../tbalden_all/devices/google devices/
          cp -r ../tbalden_all/google-modules google-modules
          cd ../
          rm -rf tbalden_all

          cd kernel_patches
          cp zuma_defconfig ../google_common_aosp_simple/arch/arm64/configs/
          cd ../

      # ------------------------------------------------------------------
      #  KernelSU + patches (unchanged)
      # ------------------------------------------------------------------
      - name: Add KernelSU
        run: |
          cd "$CONFIG"
          curl -LSs "https://raw.githubusercontent.com/KOWX712/KernelSU/master/kernel/setup.sh" | bash -s master

      - name: Apply KSU Hook Patches
        run: |
          cd "$CONFIG"
          cp ../kernel_patches/syscall_hook/min_scope_syscall_hooks_v1.5.patch ./
          patch -p1 --forward < min_scope_syscall_hooks_v1.5.patch

      - name: KernelSU version
        run: |
          cd "$CONFIG/KernelSU/kernel"
          BASE=10200
          CNT=$(git rev-list --count HEAD)
          echo "KSUVER=$((CNT + BASE))" >> "$GITHUB_ENV"

      - name: Hide-stuff patch
        run: |
          cd "$CONFIG"
          cp ../kernel_patches/69_hide_stuff.patch .
          patch -p1 -F 3 < 69_hide_stuff.patch || exit 1

      # ------------------------------------------------------------------
      #  CONFIG additions (unchanged)
      # ------------------------------------------------------------------
      - name: Add other CONFIGs
        run: |
          cd "$CONFIG"
          DEFCONFIG="arch/arm64/configs/zuma_defconfig"
          echo "CONFIG_TMPFS_XATTR=y" >> "$DEFCONFIG"
          echo "CONFIG_TMPFS_POSIX_ACL=y" >> "$DEFCONFIG"
          # … (all the other echo lines you already have) …
          echo "CONFIG_LZ4HC_COMPRESS=y" >> "$DEFCONFIG"

      # ------------------------------------------------------------------
      #  uname patch – replaces the old sed hacks
      # ------------------------------------------------------------------
      - name: Apply kernel_uname.patch (replaces all sed hacks)
        run: |
          cd "$CONFIG"
          cp ../kernel_patches/sultan/kernel_uname.patch ./
          patch -p1 --forward --fuzz=3 < kernel_uname.patch || true
          rm -f kernel_uname.patch
          perl -pi -e 's/build-timestamp = \$\(or \$\(KBUILD_BUILD_TIMESTAMP\), \$\(build-timestamp-auto\)\)/build-timestamp = "Tue Jul 8 09:17:32 UTC 2025"/' init/Makefile


      - name: Remove incompatible sweep2sleep driver (GKI incompatible)
        run: |
         cd "$CONFIG/common"
         rm -f drivers/misc/inputfilter/sweep2sleep.c
         echo "Removed sweep2sleep.c – it uses non-existent UCI interface"
         
      # ------------------------------------------------------------------
      #  BUILD – THIS IS THE ONLY PART THAT CHANGED
      # ------------------------------------------------------------------
      - name: Build the Kernel
        run: |
          cd "$CONFIG"
          export SOURCE_DATE_EPOCH=$(date -d "Tue Jul 8 09:17:32 UTC 2025" +%s)
          export KBUILD_BUILD_USER="build-user"
          export KBUILD_BUILD_HOST="build-host"
          export KBUILD_VERBOSE=1

          # ---- CRITICAL ----
          make O=$OUT_DIR ARCH=$ARCH ${{ inputs.codename }}_defconfig
          make O=$OUT_DIR ARCH=$ARCH -j$(nproc) V=1

      # ------------------------------------------------------------------
      #  (Optional) Verify the correct ar is used
      # ------------------------------------------------------------------
      - name: Debug – which ar is building net/built-in.a
        run: |
          cd "$CONFIG"
          make O=$OUT_DIR ARCH=$ARCH -n -j1 | grep -m1 "ar.*built-in.a" || true

      # ------------------------------------------------------------------
      #  Copy images (updated to use $OUT_DIR)
      # ------------------------------------------------------------------
      - name: Copy Images
        run: |
          cd "$CONFIG"
          cp ./$OUT_DIR/arch/arm64/boot/Image.lz4 ../AnyKernel3/Image.lz4
          if [ "${{ inputs.codename }}" = "gs201" ]; then
            cat ./$OUT_DIR/google-devices/gs201/dts/*.dtb > ../AnyKernel3/dtb
          elif [ "${{ inputs.codename }}" = "zuma" ]; then
            cat ./$OUT_DIR/google-devices/zuma/dts/*.dtb > ../AnyKernel3/dtb
          elif [ "${{ inputs.codename }}" = "zumapro" ]; then
            cat ./$OUT_DIR/google-devices/zumapro/dts/*.dtb > ../AnyKernel3/dtb
          fi

      # ------------------------------------------------------------------
      #  ZIP & upload (unchanged)
      # ------------------------------------------------------------------
      - name: Create ZIP Files for Different Formats
        run: |
          cd ./AnyKernel3
          ZIP_NAME="KowSU-${{ env.KSUVER }}-zuma_A16_Cleanslate-3.7.0_SUSFS-${{ inputs.release_tag }}.zip"
          zip -r "../$ZIP_NAME" ./*

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kernel-${{ inputs.codename }}-cleanslate-kowsu
          path: |
            *.zip
